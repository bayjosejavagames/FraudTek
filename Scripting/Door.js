/**
 * Created by Bayjose Java Games on 8/10/2018.
 */
var door;
var open = false;
function init(x, y){
    var entities = ScriptingEngine.getScript("EntityManager").var("entities");
    // door = entities.push(new EntityModel(ModelLoader.loadModel("quad"), "SubwayFloor", new Vector3f(x, 2, y), 0, 0, 0, 1));

}

function tick(){
    // door.rotate(0, 1, 0);
}

function unlock(){

}

function render(){

}